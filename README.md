# Basic REST service via Jakarta EE

```
mvn clean package tomee:run
```

Once the runtime starts, you can access the project at [http://localhost:8080/restfulservice](http://localhost:8080/restfulservice).

